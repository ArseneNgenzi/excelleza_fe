
name: Build and Deploy  static files

on:
  push:
    branches:
      - none

jobs:
  build:
    name: Build 
    runs-on: ubuntu-24.04

    steps:
      - name: Print a message
        run: echo "This pipeline is not in use."

      # - name: Checkout code
      #   uses: actions/checkout@v4

#       - name: Set up Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: 22.13.1

#       - name: Install dependencies
#         run: npm ci

#       - name: Build 
#         run: npm run build

#       - name: Archive build files
#         run: tar -czvf dist.tar.gz dist

#       - name: Upload artifact
#         uses: actions/upload-artifact@v4
#         with:
#           name: angular-build
#           path: dist.tar.gz


#   deploy:
#     name: Deploy to Server
#     runs-on: ubuntu-latest
#     needs: build

#     steps:
#       - name: Download build artifact
#         uses: actions/download-artifact@v4
#         with:
#           name: angular-build
#           path: .

#       - name: Copy artifact via SSH
#         uses: appleboy/scp-action@v1
#         with:
#             host: ${{ secrets.HOST }}
#             username: ${{ secrets.USERNAME }}
#             key: ${{ secrets.KEY }}
#             source: dist.tar.gz
#             target: /tmp

#       - name: Extract artifact on server
#         uses: appleboy/ssh-action@v1
#         with:
#             host: ${{ secrets.HOST }}
#             username: ${{ secrets.USERNAME }}
#             key: ${{ secrets.KEY }}
#             script: |
#                 tar -xzvf  /tmp/dist.tar.gz -C /var/www/eccellenza
#                 nginx -s reload
